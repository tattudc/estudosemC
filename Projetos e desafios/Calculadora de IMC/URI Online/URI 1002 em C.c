#include <stdio.h>
#include <stdlib.h>

int main() {
    int A, B, X;
    scanf("%i", &A);
    scanf ("%i", &B);
    X = A + B;
    printf("X e igual %i\n", X);

    system("pause");
    return 0;
}