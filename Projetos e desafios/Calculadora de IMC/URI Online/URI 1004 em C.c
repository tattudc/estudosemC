#include <stdio.h>
#include <stdlib.h>

int main() {
    int A, B, PROD;
    scanf("%i", &A);
    scanf ("%i", &B);
    PROD = A * B;
    printf("Prod = %i\n", PROD);

    system("pause");
    return 0;
}