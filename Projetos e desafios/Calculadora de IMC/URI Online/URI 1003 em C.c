#include <stdio.h>
#include <stdlib.h>

int main() {
    int A, B, SOMA;
    scanf("%i", &A);
    scanf ("%i", &B);
    SOMA = A + B;
    printf("Soma = %i\n", SOMA);

    system("pause");
    return 0;
}